# Toggl Auto-Fill CLI — Configuration
# Copy this file and fill in your real values,
# or override any setting via environment variables.

workday_hours: 8
day_start_time: "09:00"
billable: true
default_projects:
  - "Your Project Name"
time_off_project: "Time Off"
daily_standup_mins: 15

# Minimum gap (in minutes) to consider for filling and break gaps before existing entries
min_gap_mins: 15

# Maximum continuous work session length (in minutes) before forcing a break gap
max_continuous_work_mins: 180
